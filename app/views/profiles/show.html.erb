<!-- Page Heading -->
<div class="row">
    <div class="col-lg-12">
        <h1 class="page-header">
            Perfil <small><%= @profile.long_name %> de <%= @organization.name %></small>
        </h1>
        <ol class="breadcrumb">
            <li class="active">
                <i class="fa fa-dashboard"></i> <%= link_to 'Organizaciones', organization_path(@organization) %> > <%= link_to 'Perfiles', organization_profiles_path(@organization) %> > Editar
            </li>
        </ol>
    </div>
</div>
<div class="row">
  <div class="col-md-6">
  <p class="form-group">
    <strong>Long name:</strong> 
    <%= @profile.long_name %>
  </p>
  <p class="form-group">
    <strong>Name:</strong> 
    <%= @profile.name %>
  </p>
  </div>
  <div class="col-md-6"></div>
</div>

<%= link_to 'Editar', edit_organization_profile_path(@organization, @profile), class: 'btn btn-default btn-xs' %> |
<%= link_to 'Atrás', organization_profiles_path(@organization), class: 'btn btn-default btn-xs' %>